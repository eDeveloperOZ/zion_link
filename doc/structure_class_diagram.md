```markdown:doc/structure_class_diagram.md
# Structure Class Diagram

The structure of the application revolves around several key models and views. Below is a simplified class diagram and description of the main components.

## Models

### Building
- Represents a building entity.
- Contains fields for `id`, `name`, `address`, `apartments`, and `expenses`.
- Methods include `toJson` for serialization.

